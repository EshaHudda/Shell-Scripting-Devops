#!/bin/bash
# Create a new folder named "myFolder"
mkdir myFolder
# Chnage the directory to myFolder
cd myFolder
# Create new sub folders name as src, bin and docs
mkdir src bin docs
echo "Folder structure created successfuly"
pwd 
# Change the directory to the src directory
cd src
# Print the path again
pwd